<?xml version="1.0" encoding="utf-8" standalone="no"?>
<application xmlns="http://ns.adobe.com/air/application/1.0">

	<!-- The application identifier string, unique to this application. Required. -->
	<id>com.patspam.tarpo</id>

	<!-- Used as the filename for the application. Required. -->
	<filename>Tarpo</filename>

	<!-- The name that is displayed in the AIR application installer. Optional. -->
	<name>Tarpo</name>

	<!-- An application version designator (such as "v1", "2.5", or "Alpha 1"). Required. -->
	<version>1.0.2</version>

	<!-- Description, displayed in the AIR application installer. Optional. -->
	<description>Tarpo by Patrick Donelan</description>

	<!-- Copyright information. Optional -->
	<copyright>(c) 2009 - Patrick Donelan</copyright>

	<!-- Settings for the application's initial window. Required. -->
	<initialWindow>
		<!-- The main HTML file of the application. Required. -->
		<content>src/html/main.html</content>
		
		<!-- The title of the main window. Optional. -->
		 <title>Tarpo</title>

		<!-- The type of system chrome to use (either "standard" or "none"). Optional. Default standard. -->
		<systemChrome>standard</systemChrome>

		<!-- Whether the window is transparent. Only applicable when systemChrome is false. Optional. Default false. -->
		<transparent>false</transparent>

		<!-- Whether the window is initially visible. Optional. Default false. -->
		<visible>false</visible>

		<!-- The window's initial width. Optional. -->
		<width>1024</width>

		<!-- The window's initial height. Optional. -->
		<height>768</height>

		<!-- The window's initial x position. Optional. -->
		<x>200</x>

		<!-- The window's initial y position. Optional. -->
		<y>200</y>

		<!-- The window's minimum size, specified as a width/height pair, such as "400 200". Optional. -->
		<minSize>800 600</minSize>

		<!-- The window's initial maximum size, specified as a width/height pair, such as "1600 1200". Optional. -->
		<!--<maxSize>1280 1024</maxSize>-->
	</initialWindow>

	<!-- The subpath of the standard default installation location to use. Optional. -->
	<!--<installFolder>Tarpo</installFolder>-->

	<!-- The subpath of the Windows Start/Programs menu to use. Optional. -->
	<!-- <programMenuFolder>Tarpo</programMenuFolder> -->

	<!-- The icon the system uses for the application -->
	<icon>
	  	<image16x16>src/images/icons/extlogo16.png</image16x16>
		<image32x32>src/images/icons/extlogo32.png</image32x32>
		<image48x48>src/images/icons/extlogo48.png</image48x48>
		<image128x128>src/images/icons/extlogo128.png</image128x128>
	</icon>

</application>
